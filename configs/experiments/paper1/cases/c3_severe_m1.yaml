# Paper1 environment case: (C3, Severe, G2, M1)
# C3 = F1 (distance decay) + F2 (local shadow) + F3 (stochastic jitter)
extends:
  - ../../../base.yaml
  - ../../../comm_profiles/c3_severe.yaml

scene_file: configs/scenes/g2_cluster_m1.yaml

experiment:
  env_case:
    comm_case: C3
    task_case: G2
    conflict_level: M1
  comm_degradation:
    level: severe
