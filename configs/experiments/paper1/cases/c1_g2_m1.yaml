# Paper1 environment case: (C1, G2, M1)
extends:
  - ../../../base.yaml
  - ../../../comm_profiles/c1.yaml

scene_file: configs/scenes/g2_cluster_m1.yaml

experiment:
  env_case:
    comm_case: C1
    task_case: G2
    conflict_level: M1

