# Paper1 environment case: (C3, Medium, G2, M2)
# C3 = F1 (distance decay) + F2 (local shadow) + F3 (stochastic jitter)
extends:
  - ../../../base.yaml
  - ../../../comm_profiles/c3_medium.yaml

scene_file: configs/scenes/g2_cluster_m2.yaml

experiment:
  env_case:
    comm_case: C3
    task_case: G2
    conflict_level: M2
  comm_degradation:
    level: medium
