# Paper1 environment case: (C1, G2, M0)
extends:
  - ../../../base.yaml
  - ../../../comm_profiles/c1.yaml

scene_file: configs/scenes/g2_cluster_m0.yaml

experiment:
  env_case:
    comm_case: C1
    task_case: G2
    conflict_level: M0

