# Paper1 environment case: (C2-hard, G2, M2) — coupling axis pressure scenario
extends:
  - ../../../base.yaml
  - ../../../comm_profiles/c2_hard.yaml

scene_file: configs/scenes/g2_cluster_m2.yaml

experiment:
  env_case:
    comm_case: C2-hard
    task_case: G2
    conflict_level: M2
