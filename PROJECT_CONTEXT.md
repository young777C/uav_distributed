# PROJECT_CONTEXT

## 项目目标

本项目服务于 GCS-UAV 分布式双环规划论文代码验证。

核心目标是验证：在通信退化和能量约束条件下，GCS-UAV 双环协同架构能够提升任务有效完成率，并降低平均回传时延。

## 论文主线

FDCL、WCDL、CDSL 的本地自主决策能力逐步减弱。

预期总体趋势：

FDCL > WCDL > CDSL

尤其在通信退化更严重、任务聚簇与弱通信区域错位更明显时，FDCL 的优势应更明显。

## 架构设定

### FDCL

完整双环协同架构。

- GCS 负责慢环滚动规划；
- UAV 负责快环局部执行；
- GCS 能接收任务状态、通信状态、能量状态反馈；
- UAV 能基于局部状态进行执行调整；
- 任务有效完成 = 覆盖成功 + 数据成功回传。

### WCDL

弱协同架构。

- 仍具备一定反馈；
- 但通信感知能力、反馈频率或本地调整能力弱于 FDCL；
- 预期效果弱于 FDCL，但强于 CDSL。

### CDSL

低自主或中心依赖架构。

- 本地自主性最弱；
- 通信退化或任务分布不利时适应能力最弱；
- 预期整体表现最差。

## 模型约束

代码必须体现：

1. 通信质量影响任务选择；
2. 能量约束影响任务选择；
3. 返航约束影响任务选择；
4. 回传成功率影响任务有效完成；
5. 平均回传时延可以统计；
6. 快环状态机与慢环规划存在区别。

## 实验场景

通信场景：

- C1：平滑距离衰减；
- C2：局部遮挡突降 + 时变扰动。

任务场景：

- G1：均匀分布；
- G2：聚簇分布。

实验组合：

- C1 + G1；
- C1 + G2；
- C2 + G1；
- C2 + G2。

## 输出指标

核心指标：

1. 任务有效完成率；
2. 平均回传时延。

## 禁止方向

不要引入：

1. 强化学习；
2. 深度学习；
3. 复杂神经网络；
4. 机载缓存机制；
5. 与当前论文无关的新系统架构；
6. 过度复杂的优化算法。

## 修改原则

优先采用最小改动。

所有修改必须服务论文叙事，不允许为了结果好看而硬编码实验结果。
