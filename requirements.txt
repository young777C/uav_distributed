# uav-distributed runtime dependencies (Python >= 3.10)
# Install: python3 -m pip install -r requirements.txt

# --- Core (required to run simulation + sweeps) ---
numpy>=1.24          # numerical core, used everywhere
PyYAML>=6.0          # parse configs/ and scenes YAML (import yaml)
ortools>=9.8         # slow-loop task/route optimization

# --- Analysis & plotting (required for scripts/plot_*.py, analyze_*.py) ---
pandas>=2.0          # results aggregation (summary.csv) and figure data
matplotlib>=3.7      # scene / trajectory / ablation figures

# --- Optional ---
# torch is only imported (best-effort) in src/uavlab/common/seed.py to sync
# RNG seeds when available; the platform runs without it.
# torch>=2.0

# --- Dev / test ---
pytest>=7.0
